default {
  forceBump = true
}

patch {
  componentToBump = PATCH
}

minor {
  componentToBump = MINOR
}

major {
  componentToBump = MAJOR
}

newPreRelease {
  newPreRelease = true
}

promoteToRelease {
  promoteToRelease = true
}

snapshot {
  snapshot = true
}

non-snapshot {
  snapshot = false
}

tagPrefix {
  tagPrefix = "alpha."
}
